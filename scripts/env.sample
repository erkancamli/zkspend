# === Chain / wallet ===
RPC_URL=https://evmrpc-testnet.0g.ai
PRIVATE_KEY=0xYourPrivateKey
FROM=0xYourAddress
CAMPAIGN=0xYourCampaign

# === 0G Storage (real integration) ===
# OG_STORAGE_UPLOAD_CMD="og-cli put"     # gerçek CLI/SDK komutun
# OG_STORAGE_GATEWAY="https://gateway.example/ipfs"  # URL öneki (opsiyonel)
